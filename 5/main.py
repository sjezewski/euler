#!/usr/bin/env python

import math
import sys

def get_prime_factors(num):

def smallest_evenly_divisible_num(max_factor):
    factors = []
    for f in range(1, max_factor):
        factors.push(get_prime_factors(f))
    factors.uniq!
    factors.sort(:desc)

    n = 0
    biggest_prime_factor = factors[0]

    while


if __name__ == "__main__":
    print "max ed num for 1..10 = %s"%smallest_evenly_divisible_num(10)
    print "max ed num for 1..20 = %s"%smallest_evenly_divisible_num(20)

